<resources>

    <style name="Theme" parent="FrameworkRoot.Theme" />

    <style name="Theme.ApaPura" parent="Theme.ApaPura.Base">
        <item name="android:statusBarColor">@color/primary_dark</item>
    </style>

    <style name="Theme.ApaPura.WithNavDrawer" parent="Theme.ApaPura">
        <item name="android:windowTranslucentStatus">true</item>
    </style>

    <style name="MapUiWidgetStyle">
        <item name="android:background">@drawable/map_type_satellite_switcher_drawable</item>
        <item name="android:clipToPadding">false</item>
        <item name="android:elevation">2dp</item>
        <item name="android:outlineProvider">bounds</item>
    </style>
</resources>
